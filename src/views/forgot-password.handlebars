<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Recuperar contraseña</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="login-container">
    <h2>Recuperar contraseña</h2>
    <p>Ingresá tu email y nueva contraseña</p>

    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}

    {{#if success}}
      <p class="success">{{success}}</p>
    {{/if}}

    <form method="POST" action="/forgot-password">
      <input type="name" name="username" placeholder="Nombre de usuario" required />
      <input type="password" name="newPassword" placeholder="Nueva contraseña" required />
      <button type="submit">Actualizar contraseña</button>
    </form>

    <div class="register-link">
      <a href="/"><button class="register-btn">Volver al login</button></a>
    </div>
  </div>
</body>
</html>